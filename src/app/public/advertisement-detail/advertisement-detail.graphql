query getAdvertisementDetail($id: String!) {
 advertisement(uuid: $id) {
  uuid
  name
  description
  price
  createdAt
  updatedAt
  imageUrl
  published
  video
  map
  advertisementImages {
   uuid
   imageUrl
   order
  }
 }
}

