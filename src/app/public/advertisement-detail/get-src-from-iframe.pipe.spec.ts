import { GetSrcFromIframePipe } from './get-src-from-iframe.pipe';

describe('GetSrcFromIframePipe', () => {
  it('create an instance', () => {
    const pipe = new GetSrcFromIframePipe();
    expect(pipe).toBeTruthy();
  });
});
